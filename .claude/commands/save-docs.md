Save the content described by the user to the `docs/` folder of this repo as a Markdown (`.md`) file.

Steps:
1. Determine an appropriate filename from the topic or title (kebab-case, e.g. `api-authentication.md`). If the user supplied a name via $ARGUMENTS, use that instead.
2. Check if a file with that name already exists in `docs/` — if so, update it rather than overwriting without warning.
3. Write well-structured Markdown: use a top-level `#` heading, organize with `##` sections as needed, and keep content concise and accurate.
4. Confirm the file path written and give a one-line summary of what was saved.

$ARGUMENTS
